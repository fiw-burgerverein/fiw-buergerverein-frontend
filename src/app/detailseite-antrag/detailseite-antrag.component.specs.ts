/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DetailseiteAntragComponent } from './detailseite-antrag.component';

describe('DetailseiteAntragComponent', () => {
  let component: DetailseiteAntragComponent;
  let fixture: ComponentFixture<DetailseiteAntragComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DetailseiteAntragComponent ]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailseiteAntragComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
